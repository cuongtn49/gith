var link = document.createElement('link');
link.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
link.href = 'css/quiz3.css'; // Đường dẫn đến tệp CSS
function quiz1() {
    // Tạo một thẻ link mới
    // var link = document.createElement('link');

    // Cấu hình các thuộc tính của thẻ link
    link.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    link.href = 'css/quiz1.css'; // Đường dẫn đến tệp CSS

    // Thêm thẻ link vào phần head của trang
    // document.head.appendChild(link);
}
function quiz2() {
    // Tạo một thẻ link mới
    // var link = document.createElement('link');

    // Cấu hình các thuộc tính của thẻ link
    link.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    link.href = 'css/quiz2.css'; // Đường dẫn đến tệp CSS

    // Thêm thẻ link vào phần head của trang
    // document.head.appendChild(link);

}
function quiz3() {
    // Tạo một thẻ link mới
    // var link = document.createElement('link');

    // Cấu hình các thuộc tính của thẻ link
    link.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    link.href = 'css/quiz3.css'; // Đường dẫn đến tệp CSS

    // Thêm thẻ link vào phần head của trang
    // document.head.appendChild(link);

}
document.head.appendChild(link);

var linkFill = document.createElement('link');
linkFill.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
linkFill.href = 'css/fill3.css'; // Đường dẫn đến tệp CSS
function fill1(){
    linkFill.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    linkFill.href = 'css/fill1.css'; // Đường dẫn đến tệp CSS
}
function fill2(){
    linkFill.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    linkFill.href = 'css/fill2.css'; // Đường dẫn đến tệp CSS
}
function fill3(){
    linkFill.rel = 'stylesheet';  // Định nghĩa kiểu liên kết
    linkFill.href = 'css/fill3.css'; // Đường dẫn đến tệp CSS
}
document.head.appendChild(linkFill);